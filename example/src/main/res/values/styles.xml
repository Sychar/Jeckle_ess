<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->

    </style>

    <style name="AppBaseTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppBaseTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppBaseTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ViewVerfaren">
        <item name="android:shape">rectangle</item>
        <item name="android:background">#ffff</item>
        <item name="android:strokeWidth">4</item>
        <item name="android:strokeColor">@color/colorPrimary</item>
    </style>

    <style name="AppBaseTheme.sakhr" parent="AppBaseTheme.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="android:windowFullscreen">true</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowIsTranslucent">true</item>


        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

    <style name="progress" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:minHeight">20dp</item>
        <item name="android:maxHeight">30dp</item>

    </style>

    <style name="AppBaseTheme.sakhr.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppBaseTheme.sakhr.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppBaseTheme.sakhr.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
<style name="genralstyle">
    <item name="android:textStyle"> italic</item>

</style>
<style name="scroll">
    <item name="android:background">#00FFFFFF</item>
    <item name="android:textColor">#7E7474</item>
    <item name="android:enabled">false</item>
    <item name="android:layout_width"> 170dp </item>
    <item name="android:layout_height"> 68dp</item>
</style>
</resources>
